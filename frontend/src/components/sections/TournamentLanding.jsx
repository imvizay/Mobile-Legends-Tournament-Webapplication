import { Bell, ChevronDown, Trophy, Grid3X3, List } from "lucide-react";


import { useTheme } from "../../contexts/ThemeContext";
import { useThemeAsset } from "../../hooks/common/useThemeAsset";

export default function TournamentLanding() {
    const theme  = useTheme()
    const benedetta = useThemeAsset(theme)

  return (
    <section className="min-h-screen bg-[#f8f7f5] overflow-hidden">

      
    </section>
  )
}